name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - shell: pwsh
        env:
          FUN_API_KEY: ${{ secrets.SUPA_BASE_API_KEY }}
        run: |
          # example-command "$env:SUPER_SECRET"